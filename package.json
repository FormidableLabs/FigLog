{
  "name": "figlog",
  "version": "1.0.0",
  "description": "The easiest and most efficient way to document decisions and the evolution of your work in Figma.",
  "scripts": {
    "build": "esbuild widget-src/code.tsx --bundle --outfile=dist/code.js --target=es6",
    "tsc": "tsc --noEmit -p widget-src",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@figma/widget-typings": "*",
    "esbuild": "*",
    "typescript": "*"
  }
}
